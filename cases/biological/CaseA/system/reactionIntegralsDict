FoamFile
{
    format      ascii;
    class       dictionary;
    object      reactionIntegralsDict;
}

functions
{
    biologicalRates
    {
        type            volFieldValue;
        libs            (fieldFunctionObjects);

        log             true;
        writeFields     false;

        operation       weightedVolIntegrate;
        weightField     alpha.waterMean;

        fields
        (
            SsUptakeRate
            rhoHyd
            rhoH
            oxygenUptakeRate
        );
    }
}
